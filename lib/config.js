
exports.config = {
	/**
	 * Port that console should listen on
	 */
	port : 8181,
	/**
	 * Port that xtomp is listening on for STOMP protocol messages, must match xtomp.conf
	 */
	stompPort : 61613,
	/**
	 * spit logs to stdout
	 */
	debug : true
};

